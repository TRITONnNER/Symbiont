fastapi>=0.110
uvicorn[standard]>=0.29
# server.py imports BaseModel directly (`from pydantic import BaseModel`). It is
# a transitive fastapi dependency, but declare it explicitly so the direct import
# never depends on fastapi's internal pinning.
pydantic>=2,<3
cryptography>=42
# Required by starlette.testclient (smoke_test.py / selftest_*.py). Also a
# convenient runtime HTTP client. Without it the API self-tests cannot import.
httpx>=0.27
