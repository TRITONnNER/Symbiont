# Симбионт — Server-in-a-Box: узел за одну команду

Превращает чистый VPS в рабочий узел Симбионта **одной строкой**, само-регистрирует его
в списке серверов и даёт CLI управления. Это и есть «купил сервер — и всё».

---

## Одна команда (на чистом Ubuntu 22.04/24.04, от root)

```bash
curl -fsSL https://raw.githubusercontent.com/TRITONnNER/Symbiont/main/symbiont_build/server/get.sh \
  | sudo bash -s -- --register-to https://ВАШ_БЭКЕНД --node-secret СЕКРЕТ_ОПЕРАТОРА
```

- `--register-to` — адрес вашего задеплоенного бэкенда.
- `--node-secret` — операторский секрет (`SYMBIONT_NODE_SECRET` на бэкенде) — им узел подписывает саморегистрацию.
- Необязательно: `--code NL --country Netherlands --sni www.microsoft.com` (иначе авто-подбор по IP).
- До релиза в `main` можно указать ветку/источник: `SYMBIONT_REF=<branch>` или `SYMBIONT_SRC=<raw-base>`.

### Что происходит само
1. Ставит зависимости, скачивает набор узла в `/opt/symbiont`.
2. `bootstrap_vps.sh`: **авто-подбор** страны/SNI (`node_autotune.py`), установка **sing-box**
   (VLESS-Reality `443/tcp` + Hysteria2 `8443/udp` + Shadowsocks-2022 `9443/tcp`), systemd, firewall.
3. **Само-регистрация** в подписанном манифесте бэкенда (`/v1/node/register`, HMAC) →
   бэкенд пересобирает и переподписывает манифест → **узел появляется у клиентов сам**.
4. **Heartbeat** (каждые 60с): замолчал дольше `NODE_STALE_SEC` → само-починка выкидывает из
   списка; вернулся → снова появляется. Живая загрузка тоже идёт в манифест.
5. Ставит CLI **`symbiont-node`** в `PATH`.

---

## Управление узлом на сервере — `symbiont-node`

```
symbiont-node status         состояние узла и heartbeat + инфо (id, страна, протоколы)
symbiont-node logs [N] [-f]   логи sing-box (N строк; -f — следить)
symbiont-node restart         перезапустить узел
symbiont-node update          обновить до свежей версии (та же команда установки)
symbiont-node remove          снять узел (уйдёт из списка серверов по self-heal)
symbiont-node info            полный манифест узла
```

Параметры оператора хранятся в `/etc/symbiont/sib.env` (для `update`/`remove`).

---

## Панель управления — скачал файл, открыл на любом устройстве

`symbiont_build/server/sib-console.html` — **один файл, БЕЗ CDN и внешних запросов**.
Скачивается и открывается в любом браузере (ПК/ноут/планшет). Вход:

- **Владелец** — по `ADMIN_TOKEN` (полный доступ);
- **Работник** — по staff-токену с ограниченными правами (напр. только «Поддержка»).

Вкладки (появляются по правам): **Обзор** (серверы/поддержка/скидки/БД), **Серверы**
(список + снять узел), **Поддержка** (обращения + ответ оператора), **Скидки**
(создать/список/отозвать), **Блоки** (включать/выключать блоки сайта и приложения),
**Работники** (владелец создаёт токены работников с набором прав).

Всё общение — с задеплоенным бэкендом по его `/v1/admin/*` (CORS `*`, авторизация по
заголовку — панель работает даже открытая как локальный файл).

## Роли: владелец и работники (RBAC)

Владелец (держатель `ADMIN_TOKEN`) создаёт **именные токены работников** с набором прав
(scopes: `nodes`, `support`, `discounts`, `flags`, `keys`, `billing`) — как выпуск ключа.
Работник входит в панель своим токеном и видит **только выданные разделы**.

```
GET  /v1/admin/whoami                  роль + права текущего токена (панель рисует вкладки)
POST /v1/admin/staff                   владелец создаёт работника {name, scopes} → token (1 раз)
GET  /v1/admin/staff                   список работников (без токенов)
POST /v1/admin/staff/{id}/revoke       отозвать доступ работника (токен сразу мёртв)
```

## Управление флотом централизованно (оператор / панель)

Админ-эндпоинты — для панели владельца / работника (scope `nodes`):

```
GET  /v1/admin/nodes              список узлов: id, страна, host, протоколы, роли,
                                  загрузка, last_seen, alive (жив ли по heartbeat)
POST /v1/admin/node/{id}/remove   снять узел из манифеста (секрет отзывается, манифест пересобран)
```

## Поддержка: реальный диалог оператор ↔ пользователь

Никаких фейковых авто-ответов. Пользователь пишет — обращение попадает в очередь и
**ждёт оператора**. Оператор (владелец или работник со scope `support`) отвечает из
панели; ответ виден пользователю в приложении/на сайте. Тред привязан к аккаунту
(переписка видна с любого устройства пользователя).

```
GET  /v1/admin/support/threads         все обращения; ждущие ответа — сверху
GET  /v1/admin/support/thread/{key}    полная переписка одного треда
POST /v1/admin/support/{key}/reply     ответ оператора
```

## Платежи: тестовый режим «формальная покупка»

Деплой включает `SYMBIONT_PAY_PROVIDER=mock`: покупка проходит **как реальная**
(страница оплаты + подтверждение колбэком), но **деньги НЕ списываются**. Перед приёмом
реальных денег — снять `mock`, подключить боевой провайдер и вебхук `/v1/billing/webhook`.

## База данных: реальная SQLite/WAL + бэкапы

Всё состояние — в реальной БД `symbiont_state.db` (SQLite, режим WAL: атомарные
транзакции, конкурентное чтение, без полу-записанных файлов). Деплой ставит **ежедневный
онлайн-бэкап** (systemd-таймер, 03:30, снимок безопасен на живой базе, хранит 14 последних).

```
GET  /v1/admin/db/stats                путь, размер, режим журнала, ключи стейта
POST /v1/admin/db/backup               консистентный онлайн-снимок в ./backups
```

(Покрыто selftest_panel.py: RBAC владелец/работник, отказ по scope, поддержка
оператор↔пользователь, флаги, stats+бэкап, отзыв работника — 29/29.)

---

## Что нужно ДО этого (разово) — тоже одной командой

**Задеплоить бэкенд** (на отдельном VPS с доменом):

```bash
curl -fsSL https://raw.githubusercontent.com/TRITONnNER/Symbiont/main/symbiont_build/server/deploy-backend.sh \
  | sudo bash -s -- --domain api.example.com --email you@example.com
```

Ставит зависимости, код, venv, **генерит секреты** (в `/var/lib/symbiont`), поднимает
systemd-сервис (uvicorn, один воркер — состояние в памяти+SQLite), **nginx + TLS (certbot)**,
и печатает **base URL + ADMIN_TOKEN + NODE_SECRET**. Эти `NODE_SECRET`/`ADMIN_TOKEN` и подставляете
в `get.sh` (узлы) и в управление флотом. Повтор команды = обновление (идемпотентно).

> Перед приёмом реальных платежей — подключить боевой платёжный провайдер (роадмап).

Полный путь: **купил VPS → `deploy-backend.sh` (один раз) → на каждый узел `get.sh` (одна команда)**.

---

## Где это в «купить сервер — и всё»

| Шаг | Статус |
|---|---|
| Установка узла одной командой | ✅ `get.sh` |
| Авто-настройка (протоколы/страна/SNI/systemd/firewall) | ✅ `bootstrap_vps.sh` |
| Само-регистрация в списке серверов | ✅ манифест + heartbeat |
| Управление узлом на сервере | ✅ `symbiont-node` CLI |
| Управление флотом централизованно | ✅ `/v1/admin/nodes` (+remove) |
| Панель управления (скачал → открыл на любом устройстве) | ✅ `sib-console.html` (без CDN) |
| Роли: владелец + работники (RBAC по scopes) | ✅ `/v1/admin/staff` + `whoami` |
| Поддержка: реальный диалог оператор ↔ пользователь | ✅ `/v1/admin/support/*` |
| Тестовые платежи (как реальные, без списания денег) | ✅ `PAY_PROVIDER=mock` |
| Реальная БД (SQLite/WAL) + ежедневный бэкап | ✅ `persist.py` + `db_backup.py` + таймер |
| Задеплоить бэкенд + домен + секреты | ✅ `deploy-backend.sh` (одна команда) |
| Купить VPS | 🟡 за вами |
